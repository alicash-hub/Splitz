export default function App() {
  return (
    <main className="mx-auto flex min-h-full max-w-md flex-col items-center justify-center px-6 text-center">
      <h1 className="text-3xl font-semibold text-[var(--color-text)]">
        Bread &amp; Salt
      </h1>
      <p className="mt-2 text-[var(--color-text-muted)]">
        عيش وملح — split group expenses, settle up the easy way.
      </p>
      <p className="mt-8 text-sm text-[var(--color-text-muted)]">
        Project scaffolded. Build the trip flow next — see{' '}
        <code>CLAUDE.md</code>.
      </p>
    </main>
  )
}
